{
  "name": "@eightshapes/esds-icons",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run svgo && npm run svg-sprite && npm run build-es6-modules",
    "build-es6-modules": "node scrips/build-es6-modules.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "svgo": "npx svgo -f src -o dist/icons --config svgo.config.yml",
    "svg-sprite": "npx svg-sprite --config=svg-sprite.config.json dist/icons/*.svg"
  },
  "keywords": [],
  "author": "Kevin Powell <kevinmpowell@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "camelcase": "^5.3.1",
    "fs-extra": "^8.1.0",
    "svg-sprite": "^1.5.0",
    "svgo": "^1.3.2"
  }
}
